<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/action_sync_changes"
        android:icon="@drawable/ic_sync"
        android:title="Sync"
        app:showAsAction="ifRoom"
        android:visible="false"/>

    <item
        android:id="@+id/action_create_customer"
        android:title="Create customer"
        android:icon="@drawable/ic_create_customer"
        app:showAsAction="ifRoom"
        android:visible="false"/>

    <item
        android:id="@+id/action_zero_items"
        android:title="Set a SalesOrderItem to 0 quantity"
        app:showAsAction="never"
        android:visible="false"/>

    <item
        android:id="@+id/action_register"
        android:title="Login"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_unregister"
        android:title="Logout"
        app:showAsAction="never" />

</menu>
